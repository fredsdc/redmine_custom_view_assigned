<%= select "issue_assigned_to_id", "not_grouped", principals_options_for_select(users, @issue.assigned_to), {:include_blank => true, :required => @issue.required_attribute?('assigned_to_id')}, {:style => 'visibility:hidden'} %>

<script>
    //<![CDATA[
    $(function () {
        var selectedUserId = $('#issue_assigned_to_id').val();
        $('#issue_assigned_to_id').empty().html($('#issue_assigned_to_id_not_grouped').html());
        $('#issue_assigned_to_id').val(selectedUserId);
        selectedUserId = $('#issue_assigned_to_id').val();
        $('#issue_assigned_to_id').val(selectedUserId);
    })
    //]]>
</script>
